<h1>Listing Waiters</h1>

<%=  link_to "Add new Waiter", new_user_path %>

<ul>
  <% @users.each do |user| %>

    <li>(id:<%= link_to user.id, user_path(user) %>) - <%= link_to user.username, user_path(user) %></li>

  <% end %>
</ul>
