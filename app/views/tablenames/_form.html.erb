<%= form_for(@tablename) do |f| %>
  <% if @tablename.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tablename.errors.count, "error") %> prohibited this tablename from being saved:</h2>

      <ul>
      <% @tablename.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :capacity %><br>
    <%= f.number_field :capacity %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
